<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <!-- 加载ASP.NET Core插件 -->
  <extensions>
    <add assembly="NLog.Web.AspNetCore"/>
    <add assembly="NLog.MailKit"/>
  </extensions>

  <!-- 输出目的地 -->
  <targets>
    <!--系统异常发送邮件-->
    <!--<target xsi:type="Mail" name="infoMail"
            smtpServer="smtp.sina.com"
            smtpPort="25"
            smtpAuthentication="Basic"
            smtpUserName="xie_wenhao@sina.com"
            smtpPassword="xxxxxx"
            enableSsl="true"
            addNewLines="true"
            from="xie_wenhao@sina.com"
            to="592727127@qq.com"
            subject="阔界后台异常日志"
            header="==========================================="
            body="${newline}${message}${newline}"
            footer="===========================================" />-->

  <!-- 输出debug到文件 -->
    <!--<target xsi:type="File" name="allfile" fileName="c:\temp\nlog-all-${shortdate}.log"
                 layout="${longdate}|${logger}|${uppercase:${level}}|${message} ${exception}" />-->

    <!-- 另外一个日志记录文件,户口也跳过Microsoft开头相关日志信息 -->
    <!--<target xsi:type="File" name="ownFile-web" fileName="c:\temp\kjs_pay\nlog-own-${shortdate}.log"  layout="${message}" />-->
    <!--linux -->
    <target xsi:type="File" name="ownFile-web" fileName="${basedir}/logs/nlog-own-${shortdate}.log"  layout="${message}" />

    <!-- write to the void aka just remove -->
    <target xsi:type="Null" name="blackhole" />
  </targets>

  <!-- 写入目的地的规则 -->
  <rules>
    <!--All logs, including from Microsoft-->
    <!--<logger name="*" minlevel="Trace" writeTo="allfile" />-->

    <!--<logger name="*" level="Error" writeTo="infoMail" />-->

    <!--跳过Microsoft开头的相关日志信息-->
    <logger name="Microsoft.*" minlevel="Trace" writeTo="blackhole" final="true" />
    <logger name="*" minlevel="Trace" writeTo="ownFile-web" />
  </rules>
</nlog>